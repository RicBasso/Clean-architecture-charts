Clean Architecture Rings (Matplotlib)

This project visualizes Clean Architecture layers (Uncle Bob) and their practical mapping (e.g., Flutter / .NET) using concentric ring charts.

The script generates two side-by-side diagrams:
  - Clean Architecture (theory)
  - Practical Mapping (implementation)

Requirements
```bash
pip install matplotlib
```
